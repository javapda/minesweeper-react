# setup-react-project | [readme](../readme.md)

* create the base react app called `minesweeper` + install node modules
```
npx create-react-app minesweeper && cd minesweeper && npm install
```
* verify base project runs `npm start
```
npm start
```